
const {gets, print} = require('./funcoes-auxiliares');

/* 
Descontruir objetos
    const pessoa = {
        nome: 'Gabriel'
    };

    const { nome } = pessoa;

    Pegar atributo direto e como se fosse tirar ele do objeto, pra usar 
*/

print(gets());